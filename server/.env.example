# --- Datenbank ---
DATABASE_URL="postgresql://prego:CHANGE_ME@localhost:5432/prego?schema=public"

# --- App ---
PORT=3000
APP_URL="https://prego.planyvo.com"
JWT_SECRET="CHANGE_ME_64_CHARS_RANDOM"

# --- Stripe (Platform Account) ---
STRIPE_SECRET_KEY="sk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
# Preis-ID der monatlichen prego-Gebühr (Stripe Billing, im Dashboard anlegen)
STRIPE_PRICE_ID_MONTHLY="price_..."
# Plattform-Anteil pro Transaktion in Prozent
PLATFORM_FEE_PERCENT=2

# --- Web Push (einmalig generieren: npx web-push generate-vapid-keys) ---
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:hallo@prego.app"
